package com.group8project.items;

/**
 * A Document (regular reward) that can be picked up by the player
 * Increases the score of the player
 */
public class Document extends Item {
    /**
     * The default score the player receives from the document
     */
    public static final int DEFAULT_SCORE = 500;
}
